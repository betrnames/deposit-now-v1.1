# deposit.now

> The Funding Layer for AI Agents. Programmable deposits via one x402 call — fund any wallet (including sub-wallets / child agents). No humans required for secondary/agent-to-agent flows.

Machine contracts: https://deposit.now/openapi.json · https://deposit.now/llms-full.txt

## Core API

- POST https://deposit.now/api/deposit
- Body (JSON, required):
  - target: EVM address receiving net USDC (0x + 40 hex)
  - amount: net USDC to forward (decimal string, min 0.01, max 100000)
  - memo: optional string (max 256), e.g. "Fund child trading agent"
- Price: amount + 1% platform fee (agent pays gross via x402)
- Network: Base mainnet eip155:8453 (production); Base Sepolia for local dev
- Unpaid → HTTP 402 + Payment-Required (x402 v2 exact scheme, USDC)
- Paid → 200 JSON with target, depositAmount, fee, grossPaid, receiptId, receiptUrl
- After settlement: platform keeps 1%, forwards net to target via Coinbase CDP Agentic Wallet

## Example body

{"target":"0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb0","amount":"50.00","memo":"Fund child trading agent"}

## Agent pays with JavaScript

    npm install @x402/fetch @x402/core @x402/evm viem

    import { wrapFetchWithPayment } from '@x402/fetch';
    import { x402Client } from '@x402/core/client';
    import { ExactEvmScheme } from '@x402/evm/exact/client';
    import { privateKeyToAccount } from 'viem/accounts';

    const signer = privateKeyToAccount(process.env.EVM_PRIVATE_KEY); // paying agent only
    const client = new x402Client();
    client.register('eip155:*', new ExactEvmScheme(signer));
    const fetchWithPayment = wrapFetchWithPayment(fetch, client);

    const res = await fetchWithPayment('https://deposit.now/api/deposit', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        target: '0xChildWallet',
        amount: '50.00',
        memo: 'Fund child trading agent',
      }),
    });
    console.log(await res.json());

## Discovery

- GET https://deposit.now/api/discovery
- GET https://deposit.now/.well-known/x402
- GET https://deposit.now/openapi.json
- GET https://deposit.now/receipt/{id}

## Retired

- Merchant registration, /api/merchants/*, facilitator directory, merchant webhooks — gone (HTTP 410). Use POST /api/deposit only.

## Docs

- https://deposit.now/docs
- https://deposit.now/pricing — flat 1%
- https://deposit.now/llms-full.txt — extended agent guide

## Facts

- Settlement payTo (platform): 0x96da70311D3fDb8500B9AB0855E17F213dB0C9AA (Base)
- Facilitator: Coinbase CDP x402 (mainnet)
- Contact: support@deposit.now
